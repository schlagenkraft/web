apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ski-dev-certificate
  namespace: default
spec:
  secretName: ski-dev-tls
  issuerRef:
    name: ski-dev-issuer
    kind: ClusterIssuer
  commonName: ski.dev
  dnsNames:
  - ski.dev
  - '*.ski.dev'